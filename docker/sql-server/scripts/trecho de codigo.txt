
            var maior = _dadosGrid
                .Select(x => new { NomePropriedade = x.GetType().GetProperties()[0].Name, ValorPropriedade = x.GetType().GetProperties()[0].GetValue(x) })
                .ToArray();


        private static IList<TPropriedade> ObterValoresPropriedade<T, TPropriedade>(IEnumerable<T> lista, Func<T, TPropriedade> propriedade)
        {
            return lista.Select(propriedade).ToList();
        }
